// ---------------------------------------------------------------------------
// Placeholder "uploaded image".
//
// There's no real upload storage yet, so the error stage — the one stage
// that shows the raw photo rather than a recognized grid — always displays
// this same screenshot standing in for whatever the person actually
// dropped in. Swap this for the user's real uploaded file once uploads are
// actually persisted somewhere.
// ---------------------------------------------------------------------------

export const PHOTO_SRC = '/placeholder-puzzle.png';
